% Blueprint content for YoungDiagram project
% Reference: Đoković's classification of nilpotent orbits in classical real Lie groups

\chapter{Genes and Chromosomes}

\section{Gene types and genes}

\begin{definition}[Gene type]
  \label{def:GeneType}
  \lean{GeneType}
  \leanok
  A \emph{gene type} is one of three polarization types:
  $\mathtt{NonPolarized}$, $\mathtt{Positive}$, or $\mathtt{Negative}$.
  There is a natural involution $\varepsilon \mapsto -\varepsilon$ that swaps
  $\mathtt{Positive}$ and $\mathtt{Negative}$ and fixes $\mathtt{NonPolarized}$.
\end{definition}

\begin{definition}[Gene]
  \label{def:Gene}
  \lean{Gene}
  \leanok
  \uses{def:GeneType}
  A \emph{gene} $g = (n, \varepsilon)$ consists of a \emph{rank} $n \in \mathbb{N}$
  and a \emph{type} $\varepsilon \in \mathtt{GeneType}$.
\end{definition}

\begin{definition}[Gene signature]
  \label{def:Gene.signature}
  \lean{Gene.signature}
  \leanok
  \uses{def:Gene}
  The \emph{signature} of a gene $g = (n, \varepsilon)$ is a pair
  $(a, b) \in \mathbb{Q} \times \mathbb{Q}$ defined by:
  \begin{itemize}
    \item If $\varepsilon = \mathtt{NonPolarized}$: $(a,b) = (n/2,\, n/2)$.
    \item If $\varepsilon = \mathtt{Positive}$: $(a,b) = (\lceil n/2 \rceil,\, \lfloor n/2 \rfloor)$.
    \item If $\varepsilon = \mathtt{Negative}$: $(a,b) = (\lfloor n/2 \rfloor,\, \lceil n/2 \rceil)$.
  \end{itemize}
\end{definition}

\begin{lemma}[Signature sum equals rank]
  \label{lem:Gene.signature_sum_eq_rank}
  \lean{Gene.signature_sum_eq_rank}
  \leanok
  \uses{def:Gene.signature}
  For any gene $g$, we have $a(g) + b(g) = \mathrm{rank}(g)$.
\end{lemma}

\section{Chromosomes}

\begin{definition}[Chromosome]
  \label{def:Chromosome}
  \lean{Chromosome}
  \leanok
  \uses{def:Gene}
  A \emph{chromosome} is a finitely supported function $X \colon \mathtt{Gene} \to \mathbb{N}$,
  i.e.\ a formal sum of genes with non-negative integer multiplicities.
  We write $\mathtt{Chromosome} = \mathtt{Gene} \to_0 \mathbb{N}$.
\end{definition}

\begin{definition}[Chromosome signature]
  \label{def:Chromosome.signature}
  \lean{Chromosome.signature}
  \leanok
  \uses{def:Chromosome, def:Gene.signature}
  The \emph{signature} of a chromosome $X$ is the additive extension:
  $\mathrm{sig}(X) = \sum_{g} X(g) \cdot \mathrm{sig}(g) \in \mathbb{Q} \times \mathbb{Q}$.
\end{definition}

\begin{definition}[Rank]
  \label{def:Chromosome.rank}
  \lean{Chromosome.rank}
  \leanok
  \uses{def:Chromosome}
  The \emph{rank} of a chromosome $X$ is $\mathrm{rank}(X) = \sum_g X(g) \cdot \mathrm{rank}(g)$.
\end{definition}

\begin{lemma}[Signature sum equals rank for chromosomes]
  \label{lem:Chromosome.signature_sum_eq_rank}
  \lean{Chromosome.signature_sum_eq_rank}
  \leanok
  \uses{def:Chromosome.signature, def:Chromosome.rank}
  For any chromosome $X$, the sum of the two signature components equals the rank:
  $\mathrm{sig}(X).1 + \mathrm{sig}(X).2 = \mathrm{rank}(X)$.
\end{lemma}

\begin{definition}[Dominance order]
  \label{def:Chromosome.Dominates}
  \lean{Chromosome.Dominates}
  \leanok
  \uses{def:Chromosome.signature}
  For chromosomes $X$ and $Y$, we say $X$ \emph{dominates} $Y$ (written $Y \le X$) if
  $\mathrm{sig}(X^{(k)}) \le \mathrm{sig}(Y^{(k)})$ componentwise for all $k \ge 0$,
  where $X^{(k)}$ denotes the $k$-th derivative.
\end{definition}

\section{Prime and lift operations}

\begin{definition}[Prime (derivative)]
  \label{def:Chromosome.prime}
  \lean{Chromosome.prime}
  \leanok
  \uses{def:Chromosome}
  The \emph{prime} (derivative) operation $X \mapsto X'$ is the additive homomorphism
  that maps a gene $(n, \varepsilon)$ with $n \ge 1$ to $(n-1, -\varepsilon)$,
  and sends genes of rank $0$ to $0$.
\end{definition}

\begin{definition}[Lift (antiderivative)]
  \label{def:Chromosome.lift}
  \lean{Chromosome.lift}
  \leanok
  \uses{def:Chromosome}
  The \emph{lift} operation is an additive homomorphism mapping a gene
  $(n, \varepsilon)$ to $(n+1, -\varepsilon)$.
\end{definition}

\begin{lemma}[Prime-lift left inverse]
  \label{lem:prime_lift_leftInverse}
  \lean{Chromosome.prime_lift_leftInverse}
  \leanok
  \uses{def:Chromosome.prime, def:Chromosome.lift}
  The prime operation is a left inverse to lift: $(\mathrm{lift}\, X)' = X$ for all $X$.
\end{lemma}

\begin{lemma}[Max rank decreases under prime]
  \label{lem:maxRank_prime_lt}
  \lean{Chromosome.maxRank_prime_lt}
  \leanok
  \uses{def:Chromosome.prime}
  If $X \ne 0$, then $\mathrm{maxRank}(X') < \mathrm{maxRank}(X)$.
\end{lemma}

\section{Decompositions}

\begin{definition}[Below and above]
  \label{def:below_above}
  \lean{Chromosome.below, Chromosome.above}
  \leanok
  \uses{def:Chromosome}
  For $k \in \mathbb{N}$, $X_{\le k}$ (below) filters genes with rank $\le k$,
  and $X_{> k}$ (above) filters genes with rank $> k$.
  We have $X = X_{\le k} + X_{> k}$.
\end{definition}

\begin{definition}[Parity decomposition]
  \label{def:parity}
  \lean{Chromosome.oddPart, Chromosome.evenPart}
  \leanok
  \uses{def:Chromosome}
  Every chromosome decomposes as $X = X_{\mathrm{odd}} + X_{\mathrm{even}}$
  where $X_{\mathrm{odd}}$ (resp.\ $X_{\mathrm{even}}$) collects genes of odd (resp.\ even) rank.
\end{definition}

\begin{lemma}[Parity and prime interchange]
  \label{lem:parity_prime}
  \lean{Chromosome.evenPart_prime, Chromosome.oddPart_prime}
  \leanok
  \uses{def:parity, def:Chromosome.prime}
  $(X')_{\mathrm{even}} = (X_{\mathrm{odd}})'$ and $(X')_{\mathrm{odd}} = (X_{\mathrm{even}})'$.
\end{lemma}

% ================================================================

\chapter{Varieties}

\section{Filtered varieties}

\begin{definition}[Variety]
  \label{def:Variety}
  \lean{Variety}
  \leanok
  \uses{def:Chromosome}
  A \emph{variety} is an additive submonoid of $\mathtt{Chromosome}$.
\end{definition}

\begin{definition}[Filtered variety]
  \label{def:varietyOfFilter}
  \lean{Chromosome.varietyOfFilter}
  \leanok
  \uses{def:Variety}
  Given a predicate $p$ on genes, the \emph{filtered variety} $V_p$ consists of
  all chromosomes whose support satisfies $p$.
\end{definition}

\begin{lemma}[Filtered varieties are prime-stable]
  \label{lem:prime_varietyOfFilter}
  \lean{Chromosome.prime_varietyOfFilter}
  \leanok
  \uses{def:varietyOfFilter, def:Chromosome.prime}
  If $p$ is lift-stable (i.e.\ $p(g)$ iff $p(\mathrm{lift}(g))$), then the prime
  of a filtered variety is itself: $V_p' = V_p$.
\end{lemma}

\section{The polarized variety Pi}

\begin{definition}[Polarized chromosome]
  \label{def:IsPolarized}
  \lean{Chromosome.IsPolarized}
  \leanok
  \uses{def:Chromosome}
  A chromosome $X$ is \emph{polarized} if every gene in its support has type
  $\ne \mathtt{NonPolarized}$.
\end{definition}

\begin{definition}[Variety Pi]
  \label{def:Variety.Pi}
  \lean{Variety.Pi}
  \leanok
  \uses{def:varietyOfFilter, def:IsPolarized}
  The variety $\mathrm{Pi}$ is the filtered variety of all polarized chromosomes.
\end{definition}

\begin{lemma}[Pi is prime-stable]
  \label{lem:prime_Pi}
  \lean{Variety.prime_Pi}
  \leanok
  \uses{def:Variety.Pi}
  $\mathrm{Pi}' = \mathrm{Pi}$.
\end{lemma}

\begin{lemma}[Polarized signature is natural]
  \label{lem:signature_pi_isNat}
  \lean{Chromosome.signature_pi_isNat}
  \leanok
  \uses{def:Variety.Pi, def:Chromosome.signature}
  If $X \in \mathrm{Pi}$, the signature components of $X$ are natural numbers.
\end{lemma}

\begin{lemma}[Sigma sequence determines element of Pi]
  \label{lem:eq_of_sigma_eq}
  \lean{Chromosome.eq_of_sigma_eq}
  \leanok
  \uses{def:Variety.Pi, def:Chromosome.signature, def:Chromosome.prime}
  If $X, Y \in \mathrm{Pi}$ satisfy $\mathrm{sig}(X^{(k)}) = \mathrm{sig}(Y^{(k)})$ for all $k \ge 0$,
  then $X = Y$.
\end{lemma}

\begin{lemma}[Antisymmetry of dominance on Pi]
  \label{lem:pi_chromosome_antisymm}
  \lean{Chromosome.pi_chromosome_antisymm}
  \leanok
  \uses{def:Variety.Pi, def:Chromosome.Dominates}
  For $X, Y \in \mathrm{Pi}$, if $X \le Y$ and $Y \le X$, then $X = Y$.
  Hence $\mathrm{Pi}$ carries a partial order.
\end{lemma}

\section{The non-polarized variety Lambda}

\begin{definition}[Non-polarized chromosome]
  \label{def:IsNonPolarized}
  \lean{Chromosome.IsNonPolarized}
  \leanok
  \uses{def:Chromosome}
  A chromosome is \emph{non-polarized} if every gene in its support has type
  $\mathtt{NonPolarized}$.
\end{definition}

\begin{definition}[Variety Lambda]
  \label{def:Variety.Lambda}
  \lean{Variety.Lambda}
  \leanok
  \uses{def:varietyOfFilter, def:IsNonPolarized}
  The variety $\mathrm{Lambda}$ is the filtered variety of all non-polarized chromosomes.
\end{definition}

\begin{lemma}[Lambda is prime-stable]
  \label{lem:prime_Lambda}
  \lean{Variety.prime_Lambda}
  \leanok
  \uses{def:Variety.Lambda}
  $\mathrm{Lambda}' = \mathrm{Lambda}$.
\end{lemma}

\section{Mixed varieties and labels}

\begin{definition}[Mixed variety]
  \label{def:Variety.Mix}
  \lean{Variety.Mix}
  \leanok
  \uses{def:Variety}
  Given a pair of varieties $(V_1, V_2)$, the \emph{mixed variety} $\mathrm{Mix}(V_1, V_2)$
  consists of all chromosomes whose odd part lies in $V_1$ and even part lies in $V_2$.
\end{definition}

\begin{definition}[Five labeled varieties]
  \label{def:Variety.Label}
  \lean{Variety.Label}
  \leanok
  \uses{def:Variety.Pi, def:Variety.Lambda, def:Variety.Mix}
  The five labeled varieties $V_0, \ldots, V_4$ indexed by $\mathrm{Fin}\,5$ are:
  \begin{itemize}
    \item $V_0 = \mathrm{Pi}$
    \item $V_1 = \mathrm{Mix}(\mathrm{Pi}, \mathrm{Lambda})$
    \item $V_2 = \mathrm{Lambda}$
    \item $V_3 = \mathrm{Mix}(\mathrm{Lambda}, \mathrm{Pi})$
    \item $V_4 = \mathrm{Mix}(\mathrm{Lambda}, \mathrm{Lambda})$
  \end{itemize}
\end{definition}

\begin{definition}[Label prime permutation]
  \label{def:Label.prime}
  \lean{Variety.Label.prime}
  \leanok
  The prime operation on varieties induces a permutation of the five labels:
  $V_i' = V_{\pi(i)}$.
\end{definition}

% ================================================================

\chapter{Sigma Sequences}

\begin{definition}[Sigma sequence]
  \label{def:Sigma.sigma}
  \lean{Sigma.sigma}
  \leanok
  \uses{def:Chromosome.signature, def:Chromosome.prime}
  For a chromosome $X$, the \emph{sigma sequence} is the function
  $\sigma_X(k) = \mathrm{sig}(X^{(k)}) \in \mathbb{Q} \times \mathbb{Q}$.
  We write $a_X(k) = \sigma_X(k).1$ and $b_X(k) = \sigma_X(k).2$.
\end{definition}

\begin{lemma}[Sigma is antitone]
  \label{lem:Sigma.antitone}
  \lean{Sigma.antitone}
  \leanok
  \uses{def:Sigma.sigma}
  The sigma sequence is antitone (componentwise decreasing).
\end{lemma}

\begin{lemma}[Sigma is eventually zero]
  \label{lem:Sigma.eventually_zero}
  \lean{Sigma.eventually_zero}
  \leanok
  \uses{def:Sigma.sigma}
  There exists $K$ such that $\sigma_X(k) = 0$ for all $k \ge K$.
\end{lemma}

\begin{lemma}[Interlacing conditions]
  \label{lem:Sigma.interlacing}
  \lean{Sigma.cond_15_4, Sigma.cond_15_5}
  \leanok
  \uses{def:Sigma.sigma}
  The components of the sigma sequence satisfy interlacing inequalities.
  If $k$ is even, then $b_X(k+1) \le a_X(k)$ and $a_X(k+1) \le b_X(k)$.
  If $k$ is odd, the inequalities are reversed.
\end{lemma}

\begin{lemma}[Difference conditions for Pi]
  \label{lem:Sigma.cond_15_6}
  \lean{Sigma.cond_15_6}
  \leanok
  \uses{def:Sigma.sigma, def:Variety.Pi}
  If $X \in \mathrm{Pi}$, then $a_X(k) - b_X(k+1) \le 1$ and $b_X(k) - a_X(k+1) \le 1$.
\end{lemma}

\begin{lemma}[Dominance implies componentwise inequality]
  \label{lem:Sigma.cond_15_8}
  \lean{Sigma.cond_15_8}
  \leanok
  \uses{def:Sigma.sigma, lem:pi_chromosome_antisymm}
  If $X < Y$ in $\mathrm{Pi}$, then $\sigma_X(k) \le \sigma_Y(k)$ componentwise for all $k$
  with strict inequality for some $k$.
\end{lemma}

% ================================================================

\chapter{Mutations}

\section{Definition of mutations}

\begin{definition}[Mutation]
  \label{def:IsMutation}
  \lean{IsMutation}
  \leanok
  \uses{def:Chromosome.signature, def:Chromosome.Dominates}
  A pair $(X, Y)$ of chromosomes forms a \emph{mutation} if:
  \begin{enumerate}
    \item $X \ne Y$,
    \item $\mathrm{sig}(X) = \mathrm{sig}(Y)$,
    \item $Y \le X$ (dominance).
  \end{enumerate}
\end{definition}

\section{Primitive mutations on Pi}

\begin{definition}[Primitive mutations of type 1]
  \label{def:Pi.X1Y1}
  \lean{Variety.Pi.X1, Variety.Pi.Y1}
  \leanok
  \uses{def:Variety.Pi}
  Given $\varepsilon$, $k \le m$: $X_1 = (m, \varepsilon) + (k, -\varepsilon)$ and
  $Y_1 = (m+1, -\varepsilon) + (k-1, \varepsilon)$ (when $k \ge 1$).
\end{definition}

\begin{definition}[Primitive mutations of type 2]
  \label{def:Pi.X2Y2}
  \lean{Variety.Pi.X2, Variety.Pi.Y2}
  \leanok
  \uses{def:Variety.Pi}
  Given $\varepsilon$, $k \le m$: $X_2 = (m, \varepsilon) + (k, \varepsilon)$ and
  $Y_2 = (m+1, \varepsilon) + (k-1, \varepsilon)$ (when $k \ge 1$).
\end{definition}

\begin{definition}[Primitive mutations of type 3]
  \label{def:Pi.X3Y3}
  \lean{Variety.Pi.X3, Variety.Pi.Y3}
  \leanok
  \uses{def:Variety.Pi}
  Given $\varepsilon$, $k \le m$: $X_3 = (m, \varepsilon) + (k, -\varepsilon)$ and
  $Y_3 = (m+2, -\varepsilon) + (k-2, \varepsilon)$ (when $k \ge 2$).
\end{definition}

\begin{definition}[Primitive mutation relation]
  \label{def:Pi.Primitive}
  \lean{Variety.Pi.Primitive}
  \leanok
  \uses{def:Pi.X1Y1, def:Pi.X2Y2, def:Pi.X3Y3}
  A \emph{primitive mutation} on $\mathrm{Pi}$ is one of the three types above (up to adding a common chromosome $Z$).
\end{definition}

\begin{definition}[Step relation]
  \label{def:Pi.Step}
  \lean{Variety.Pi.Step}
  \leanok
  \uses{def:Pi.Primitive}
  A \emph{step} on $\mathrm{Pi}$ is a primitive mutation plus an arbitrary summand:
  $\mathrm{Step}(X+Z, Y+Z)$ whenever $\mathrm{Primitive}(X, Y)$.
\end{definition}

\section{Properties of mutations on Pi}

\begin{lemma}[Primitive mutations are mutations]
  \label{lem:Pi.Primitive.isMutation}
  \lean{Variety.Pi.Primitive.isMutation}
  \leanok
  \uses{def:Pi.Primitive, def:IsMutation}
  Every primitive mutation satisfies the mutation conditions.
\end{lemma}

\begin{lemma}[Type 1 signature preservation]
  \label{lem:mutation_type1_signature_eq}
  \lean{Chromosome.mutation_type1_signature_eq}
  \leanok
  \uses{def:Pi.X1Y1, def:Chromosome.signature}
  Type 1 mutations preserve the signature at all levels:
  $\mathrm{sig}(X_1^{(k)}) = \mathrm{sig}(Y_1^{(k)})$ for all $k$.
\end{lemma}

\begin{lemma}[Type 1 dominance]
  \label{lem:mutation_type1_le}
  \lean{Chromosome.mutation_type1_le}
  \leanok
  \uses{def:Pi.X1Y1, def:Chromosome.Dominates}
  Type 1 mutations satisfy $Y_1 \le X_1$.
\end{lemma}

\begin{lemma}[Type 2 signature preservation]
  \label{lem:mutation_type2_signature_eq}
  \lean{Chromosome.mutation_type2_signature_eq}
  \leanok
  \uses{def:Pi.X2Y2, def:Chromosome.signature}
  Type 2 mutations preserve the signature at all levels.
\end{lemma}

\begin{lemma}[Type 2 dominance]
  \label{lem:mutation_type2_le}
  \lean{Chromosome.mutation_type2_le}
  \leanok
  \uses{def:Pi.X2Y2, def:Chromosome.Dominates}
  Type 2 mutations satisfy $Y_2 \le X_2$.
\end{lemma}

\begin{lemma}[Type 3 signature preservation]
  \label{lem:mutation_type3_signature_eq}
  \lean{Chromosome.mutation_type3_signature_eq}
  \leanok
  \uses{def:Pi.X3Y3, def:Chromosome.signature}
  Type 3 mutations preserve the signature at all levels.
\end{lemma}

\begin{lemma}[Type 3 dominance]
  \label{lem:mutation_type3_le}
  \lean{Chromosome.mutation_type3_le}
  \leanok
  \uses{def:Pi.X3Y3, def:Chromosome.Dominates}
  Type 3 mutations satisfy $Y_3 \le X_3$.
\end{lemma}

\section{Main mutation theorem}

\begin{theorem}[Existence of mutations]
  \label{thm:exists_mutation_le}
  \lean{exists_mutation_le}
  \leanok
  \uses{def:Variety.Pi, def:Pi.Step, def:Chromosome.Dominates, lem:eq_of_sigma_eq, lem:pi_chromosome_antisymm}
  For any $X, Y \in \mathrm{Pi}$ with $\mathrm{rank}(X) = \mathrm{rank}(Y) = n$ and $Y \le X$,
  either $X = Y$ or there exists a step mutation from $X$ towards $Y$:
  there exist $X' \in \mathrm{Pi}$ with $\mathrm{Step}(X, X')$ and $Y \le X' < X$.
\end{theorem}

% ================================================================

\chapter{Lifting Theorem}

\begin{lemma}[Mutation lifting]
  \label{lem:mutation_lifting}
  \lean{Variety.mutation_lifting}
  \uses{def:IsMutation, def:Variety.Label, def:Chromosome.lift}
  Given a mutation $(X, Y)$ in variety $V_i$ with $X' = Y'$,
  there exists $Z \in V_{\pi(i)}$ such that $(Z, Y)$ is a mutation in $V_{\pi(i)}$
  and $Z' = X$.
\end{lemma}

% ================================================================

\chapter{Lie Algebra Connection}

\section{Classical Lie groups}

\begin{definition}[Series index]
  \label{def:SeriesIndex}
  \lean{SeriesIndex}
  \leanok
  The five classical series of real Lie groups are indexed as:
  $\mathrm{U}$ (unitary), $\mathrm{O}$ (orthogonal), $\mathrm{O}^*$ (split orthogonal),
  $\mathrm{Sp}_{\mathbb{R}}$ (real symplectic), $\mathrm{Sp}_{\mathbb{H}}$ (quaternionic symplectic).
\end{definition}

\begin{definition}[Associated variety]
  \label{def:SeriesIndex.variety}
  \lean{SeriesIndex.variety}
  \leanok
  \uses{def:SeriesIndex, def:Variety.Label}
  Each series index $j$ is associated with one of the five labeled varieties $V_i$.
\end{definition}

\section{Nilpotent orbits}

\begin{definition}[Nilpotent block]
  \label{def:NilpotentBlock}
  \lean{NilpotentBlock}
  \leanok
  \uses{def:SeriesIndex}
  A \emph{nilpotent block} for series $j$ consists of a size $m$ and a polarization type,
  subject to validity constraints depending on $j$.
\end{definition}

\begin{definition}[Nilpotent type]
  \label{def:NilpotentType}
  \lean{NilpotentType}
  \leanok
  \uses{def:NilpotentBlock}
  A \emph{nilpotent type} for series $j$ is a finitely supported function
  $\Delta \colon \mathtt{NilpotentBlock}\, j \to \mathbb{N}$, i.e.\ a formal sum of nilpotent blocks.
\end{definition}

\begin{definition}[Block to chromosome]
  \label{def:NilpotentBlock.toChromosome}
  \lean{NilpotentBlock.toChromosome}
  \leanok
  \uses{def:NilpotentBlock, def:Chromosome}
  Each nilpotent block maps to a chromosome via iterated lifting.
\end{definition}

\begin{definition}[Type to chromosome]
  \label{def:NilpotentType.toChromosome}
  \lean{NilpotentType.toChromosome}
  \leanok
  \uses{def:NilpotentType, def:NilpotentBlock.toChromosome}
  The chromosome of a nilpotent type is the additive extension of the block-to-chromosome map.
\end{definition}

\begin{theorem}[Valid blocks land in the correct variety]
  \label{thm:toChromosome_mem_variety}
  \lean{NilpotentBlock.toChromosome_mem_variety}
  \uses{def:NilpotentBlock.toChromosome, def:SeriesIndex.variety}
  For a valid nilpotent block $b$ of series $j$, we have $\mathrm{toChromosome}(b) \in V_j$.
\end{theorem}

\begin{theorem}[Valid types land in the correct variety]
  \label{thm:NilpotentType.toChromosome_mem_variety}
  \lean{NilpotentType.toChromosome_mem_variety}
  \uses{def:NilpotentType.toChromosome, def:SeriesIndex.variety, thm:toChromosome_mem_variety}
  For a valid nilpotent type $\Delta$ of series $j$,
  $\mathrm{toChromosome}(\Delta) \in V_j$.
\end{theorem}

\section{Orbit closure}

\begin{definition}[Classical setup]
  \label{def:ClassicalSetup}
  \lean{ClassicalSetup}
  \uses{def:SeriesIndex}
  A \emph{classical setup} for series $j$ bundles together the base field, vector space,
  bilinear/sesquilinear form, and the associated Lie algebra and group.
\end{definition}

\begin{theorem}[Orbit closure iff dominance]
  \label{thm:orbit_closure_iff_dominance}
  \lean{orbit_closure_iff_dominance}
  \uses{def:ClassicalSetup, def:NilpotentType.toChromosome, def:Chromosome.Dominates, thm:exists_mutation_le}
  The closure of a nilpotent orbit contains another orbit if and only if the
  corresponding chromosomes satisfy the dominance relation.
\end{theorem}
